# LTL -> PA 实现方案的确定
## 我们认为 epmc 中已经实现此过程，但是由于没有找到相应的测试用例来验证，看代码也没有发现。 [待确认]
## 本周学习了 spot 的使用方法
```
    1. 主要学习ltl到pa 的命令使用 以及 自动机描述规范hoa 的学习
        生成hoa格式的命令： ltl2tga -f "ltl-formula" --parity="conditions"
        因为对spot工具使用不熟练和omega自动机背景知识不完整，中间遇到了一些问题。
        Q1: 虽然加了 --parity 选项， 但是输出的hoa格式中 指定的自动机类型 不全是 parity
            不知到epmc中hoaparser是否做了处理，有待进一步确认？
            确认方案：
                一、阅读hoaparser的关键代码，理解其读取逻辑，读取结果是怎么转化的 [实现比较耗时]
                二、 根据命令行生成的不同输出(自动机类型不同)， 在测试框架下，编写新的测试函数，分别进行调试，确认hoaparser解析结果，比对实际类型是否一致 [此方案相对可行]
                    如果不一致，考虑怎么等价转换为 PA的形式的实现方案 [算法要求 LTL一定要转为PA 才能进行下一步Product]

        Q2: hoa格式学习： 语法可以理解，但是语义没有完全理解。
            已和李勇师兄讨论过，需要进一步对自动机知识的补充和hoa文档的研究才能更好理解。
    2. 明确需求： 我们LTL-PA真正需要做的是 根据ltl公式转成 PA，然后交给下一步跟QMC模型做product。
        具体方案： 
            step 1: spot 根据ltl生成中间格式(hoa) 
            step 2: epmc中的hoaparser读取生成我们想要的PA
                需要确认：
                    目前epmc中定义的PA数据结构是否满足我们算法的需求？
                        确认方案： 需要仔细阅读epmc关于PA的数据结构定义。
                        - 如果满足：
                            需要确认：epmc中是否已经实现了从hoa读取得到PA的过程？
                                - 确认方案： 
                                    一、咨询epmc开发或测试人员是否清楚该过程，是否可以提供测试用例供我们使用？？？
                                    二、测试框架下调试 [耗时]
                                - 如果实现了：复用即可。
                                - 如果没有：需要改写hoaParser，改写方案待定
                        - 如果不满足： 
                            step 1: 需要根据算法需求 重新设计或继承原来的数据结构
                            step 2: 改写hoaParser,解析为新的数据结构 [耗时]

```    
## 总结 
我们需要**epmc开发或测试人员**是否可以明确一下epmc**是否已经实现** ltl 公式到目前epmc中PA数据结构的转换过程？ 
如果实现了，请提供一个**测试用例**给我们!!!

# getBSCC算法实现方案
```
    之前已经确定线性代数库的使用方案。
    在初步探索后，参考了冯元老师实现过的C#代码，很有借鉴意义！
    确定getBSCC算法实现方案
        理解算法的过程中 完成getBSCC C#版 到 python版的迁移
            迁移过程中，会对C#代码进行理解，对迁移后的代码进行测试！
            中间遇到问题会及时同冯元老师交流。

```

